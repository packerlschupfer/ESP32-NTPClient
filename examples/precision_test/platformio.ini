[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino

; Use local library source directly - point to the parent directory containing library.json
lib_deps =
    NTPClient=symlink://../..

; Enable debug output
build_flags =
    -DNTP_DEBUG

monitor_speed = 921600
